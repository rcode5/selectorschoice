%nav.site_nav
  - if signed_in?
    %ul
      - [['dashboard', admin_index_path], ['tracks', admin_tracks_path]].each do |name, path|
        - selected = (request.path == path) ? 'selected' : nil
        %li{ class: selected }
          = link_to name, path
      %li
        = link_to 'sign out', sign_out_path, method: :delete
