%nav.site_nav
  - if signed_in?
    %ul
      - [['dashboard', admin_index_path], ['tracks', admin_tracks_path], ['main site', root_path]].each do |name, path|
        - selected = (request.path == path) ? 'selected' : nil
        %li{ class: selected }
          %a{href: path} #{name}
